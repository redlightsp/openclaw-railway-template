[build]
builder = "dockerfile"

[deploy]
healthcheckPath = "/setup/healthz"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
startCommand = "bash /app/setup-composio-railway.sh; echo '=== STARTING NODE ==='; node src/server.js 2>&1"

[variables]
PORT = "8080"
